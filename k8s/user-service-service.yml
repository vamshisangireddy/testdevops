apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  type: ClusterIP # Changed from LoadBalancer for internal communication
  selector:
    app: user-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5002